# Core training/runtime dependencies
torch>=1.12
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0

# Evaluation and VUS metric dependencies
scipy>=1.7
statsmodels>=0.13
matplotlib>=3.5
tqdm>=4.64
tsfresh>=0.20
hurst>=0.0.5
arch>=5.3

# Note: tensorflow is intentionally omitted because utils/logger.py
# is not used by the current training/testing entrypoints.
